{% import '../macros/render.twig' as render %}
<!DOCTYPE html>
<html>
  <head>
    {{ render.partial('head') }}
    {{ render.partial('stylesheets') }}
  </head>
  <body>
    {{ render.partial('header') }}

    {% block content %}{% endblock %}

    {{ render.partial('scripts') }}
    {{ render.partial('footer') }}
  </body>
</html>
